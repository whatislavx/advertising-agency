# Інструкція з запуску проекту Advertising Agency

Цей документ описує кроки для розгортання та запуску проекту локально за допомогою Docker.

## 📋 Вимоги

Перед початком переконайтеся, що у вас встановлено:
- [Docker](https://www.docker.com/products/docker-desktop)
- [Docker Compose](https://docs.docker.com/compose/install/) (зазвичай входить до складу Docker Desktop)

## 🚀 Швидкий старт

1. **Клонуйте репозиторій:**
   ```bash
   git clone <URL_вашого_репозиторію>
   cd advertising-agency
   ```

2. **Запустіть контейнери:**
   Виконайте команду в корені проекту (там, де лежить `docker-compose.yml`):
   ```bash
   docker-compose up --build
   ```
   
   *Прапор `--build` гарантує, що образи будуть перезібрані з останніми змінами коду.*

   Для запуску у фоновому режимі додайте прапор `-d`:
   ```bash
   docker-compose up --build -d
   ```

3. **Зупинка проекту:**
   ```bash
   docker-compose down
   ```
   Якщо ви хочете також видалити томи з даними (бази даних):
   ```bash
   docker-compose down -v
   ```

## 🌐 Доступ до сервісів

Після успішного запуску сервіси будуть доступні за наступними адресами:

| Сервіс | URL | Опис |
|--------|-----|------|
| **Frontend** | http://localhost:80 | Головна сторінка веб-додатку |
| **Backend API** | http://localhost:3000 | API сервер |

## 🛠 Технічні деталі

Проект складається з наступних контейнерів:

- **ag_frontend**: Nginx сервер, що роздає статику (HTML/CSS/JS). Порт `80`.
- **ag_backend**: Node.js (Express) сервер. Порт `3000`.
- **ag_postgres**: PostgreSQL база даних.
- **ag_mongo**: MongoDB база даних (для звітів/логів).
- **ag_redis**: Redis для кешування.

### Налаштування бази даних (PostgreSQL)
Дані для підключення (задані в `docker-compose.yml`):
- **Host**: `localhost` (або `postgres` всередині мережі docker)
- **Port**: `5432` (не прокинутий на хост за замовчуванням, доступний лише для backend)
- **User**: `user`
- **Password**: `password`
- **Database**: `agency`

### Корисні команди

Перегляд логів:
```bash
docker-compose logs -f
```

Перегляд логів конкретного контейнера (наприклад, бекенду):
```bash
docker-compose logs -f backend
```

Перезапуск конкретного сервісу:
```bash
docker-compose restart backend
```

Вхід в консоль контейнера:
```bash
docker exec -it ag_backend sh
# або
docker exec -it ag_postgres psql -U user -d agency
```
